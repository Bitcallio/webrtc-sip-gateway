#!/usr/bin/env bash
if [ "${EUID:-$(id -u)}" -ne 0 ] || [ -z "${SUDO_USER:-}" ]; then
  echo "bitcall-gateway must be run with sudo by a non-root user." >&2
  exit 1
fi

set -euo pipefail

SCRIPT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
exec node "$SCRIPT_DIR/../src/index.js" "$@"
